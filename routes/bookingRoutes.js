const express = require("express");
const router = express.Router();

// Placeholder route for now
router.get("/", (req, res) => res.json({ message: "Booking routes OK (placeholder)" }));

module.exports = router;

